---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  importDerivedRoles:
    - common_roles
  resource: "profile:skill"
  rules:
    - actions: ['*']
      effect: EFFECT_ALLOW
      roles:
        - Admin
      derivedRoles:
        - owner
